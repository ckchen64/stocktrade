export interface CandleData {
  date: string;

  open: number;
  high: number;
  low: number;
  close: number;
  volume: number;

  // =========================================================
  // 이동평균
  // =========================================================
  sma05?: number;
  sma20?: number;
  sma60?: number;

  // =========================================================
  // MACD
  // =========================================================
  macd?: number;
  macdSignal?: number;
  macdHist?: number;

  // =========================================================
  // RSI
  // =========================================================
  rsi?: number;

  // =========================================================
  // OBV
  // =========================================================
  obv?: number;

  // =========================================================
  // MFI
  // =========================================================
  mfi?: number;
  mfiSignal?: number;

  // =========================================================
  // SIGMA
  // =========================================================
  sigma?: number;
  sigmaSignal?: number;

  // =========================================================
  // ADX / DMI
  // =========================================================
  adx?: number;
  diPlus?: number;
  diMinus?: number;

  // =========================================================
  // CCI
  // =========================================================
  cci?: number;
  cciSignal?: number;

  // =========================================================
  // EOM
  // =========================================================
  eom?: number;
}

export interface ChartProps {
  chartDataList: CandleData[];
}